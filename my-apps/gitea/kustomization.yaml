apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: gitea
    repo: https://dl.gitea.io/charts/
    releaseName: gitea
    version: 12.0.0
    namespace: gitea
    valuesFile: values.yaml

resources:
  - namespace.yaml
  - secret.yaml
